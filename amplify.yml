version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - amplify pull --appId d3kwo5epm7p0px --envName dev --yes
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
