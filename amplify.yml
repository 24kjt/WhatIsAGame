version: 1
backend:
  phases:
    build:
      commands:
        - amplify pull --appId d3pq2xgxk4bx7e --envName dev --yes
    postBuild:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - amplify pull --appId d3pq2xgxk4bx7e --envName dev --yes
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
